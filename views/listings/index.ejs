<% layout("/layouts/boilerplate") %>
<!-- <h3>All Listings</h3> -->
<!-- <form method="get" action="/listings/new">
    <button>Add new Property</button>
  </form> -->

<style>
  #filters {
    
    display: flex;
    flex-wrap: wrap;
    align-items: center;
  }

  .filter {
    text-align: center;
    margin-right: 3rem;
    opacity: 0.7;
    transition: opacity 0.3s ease 0.2s;
  }

  .filter:hover {
    opacity: 1;
    cursor: pointer;
  }

  .filter p {
    font-size: 0.8rem;
  }

  .tax-info {
    display: none;

  }

  .tax-toggle {
    border: 1px solid black;
    border-radius: 1rem;
    height: 3rem;
    padding: 1rem;
    margin-left: 5rem;
    display: flex;
    align-items: center
    ;
  }
</style>

<nav class="navbar bg-body-tertiary mt-3">
  <div class="container-fluid">
    <div id="filters">
      <div class="filter">
        <div><i class="fa-solid fa-fire"></i></div>
        <p>Trending</p>
      </div>
      <div class="filter">
        <div><i class="fa-solid fa-door-open"></i></div>
        <p>Rooms</p>
      </div>
      <div class="filter">
        <div><i class="fa-solid fa-tree-city"></i></div>
        <p>Iconic Cities</p>
      </div>
      <div class="filter">
        <div><i class="fa-solid fa-mountain-sun"></i></div>
        <p>Mountains</p>
      </div>
      <div class="filter">
        <div><i class="fa-brands fa-fort-awesome-alt"></i></div>
        <p>Castles</p>
      </div>
      <div class="filter">
        <div><i class="fa-solid fa-water-ladder"></i></div>
        <p>Pools</p>
      </div>
      <div class="filter">
        <div><i class="fa-solid fa-tents"></i></div>
        <p>Camping</p>
      </div>
      <div class="filter">
        <div><i class="fa-solid fa-wheat-awn"></i></div>
        <p>Farms</p>
      </div>
      <div class="filter">
        <div><i class="fa-solid fa-snowman"></i></div>
        <p>Arctic</p>
      </div>
      
      <div class="tax-toggle">
        <div class="form-check-reverse form-switch">
          <input
            class="form-check-input"
            type="checkbox"
            role="switch"
            id="switchCheckDefault"
          />
          <label class="form-check-label" for="switchCheckDefault"
            >Display total after taxes</label
          >
        </div>
      </div>
    </div>
  </div>
</nav>

<div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 mt-3">
  <% for (let listing of listings) { %>
  <a href="/listings/<%= listing._id %>" class="listing-link">
    <div class="card col listing-card">
      <img
        src="<%= listing.image.url %>"
        class="card-img-top"
        alt="listing_image"
        style="height: 20rem"
      />
      <div class="card-img-overlay"></div>
      <div class="card-body">
        <p class="card-text">
          <b><%= listing.title %></b><br />
          &#8377; <span class="price-amount" data-base-price="<%= listing.price %>"><%= listing.price.toLocaleString("en-IN") %></span> /night
          <i class="tax-info"> &nbsp; &nbsp; (+18% GST)</i>
        </p>
      </div>
    </div>
  </a>
  <% } %>
</div>

<script>
  const priceSwitch = document.getElementById("switchCheckDefault");

  function updatePrices(includeTax) {
    const priceElements = document.getElementsByClassName("price-amount");
    for (const el of priceElements) {
      const basePrice = Number(el.dataset.basePrice);
      if (Number.isNaN(basePrice)) continue;
      const finalPrice = includeTax ? basePrice * 1.18 : basePrice;
      el.textContent = Math.round(finalPrice).toLocaleString("en-IN");
    }
  }

  function updateTaxInfoVisibility(show) {
    const taxInfoEls = document.getElementsByClassName("tax-info");
    for (const info of taxInfoEls) {
      info.style.display = show ? "inline" : "none";
    }
  }

  // Initialize prices based on current switch state
  updatePrices(priceSwitch.checked);
  updateTaxInfoVisibility(priceSwitch.checked);

  priceSwitch.addEventListener("change", (e) => {
    updatePrices(e.target.checked);
    updateTaxInfoVisibility(e.target.checked);
  });
</script>
